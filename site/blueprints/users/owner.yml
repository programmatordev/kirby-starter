title: role.owner.title
description: role.owner.description

permissions:
  access:
    languages: false
    system: false
  user:
    changeRole: false
    delete: false

# hide the default notice
# change if you want to add more fields related to a role
fields:
  hidden: true
